@import "styles/iconfont.wxss";
page, view, text, swiper, swiper-item, image, navigator {
    padding: 0rpx;
    margin: 0rpx;
    box-sizing: border-box;
}

page {
    /* 主题颜色 */
    --themeColor: #00ffff;
    
    font-size: 36rpx;
}